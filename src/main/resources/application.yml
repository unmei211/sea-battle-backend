spring:
  flyway:
    locations: classpath:migrations
    url: jdbc:postgresql://localhost:5432/sea_battle
    user: postgres
    password: cisco
  datasource:
    url: jdbc:postgresql://localhost:5432/sea_battle
    username: postgres
    password: cisco
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
